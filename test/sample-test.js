const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("Staking contract ", function () {
  it("Balance should be assigned to the owner", async function () {
    const [owner] = ethers.getSigners()
    // console.log(owner)
    // console.log(stakers1)



  });
});
